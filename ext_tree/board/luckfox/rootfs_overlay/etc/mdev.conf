# Basic devices
null 0:0 666
zero 0:0 666
random 0:0 644
urandom 0:0 644

# USB Audio hotplug - match every ALSA card; the handler filters USB-backed cards.
# * = exec on both create and remove (ACTION will be passed via environment)
card[0-9]+ 0:0 664 */usr/bin/usb-audio-hotplug.sh